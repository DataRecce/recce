// ============================================================================
// Run Types - Re-exported from @datarecce/ui/api
// ============================================================================

/**
 * Re-export Run types from @datarecce/ui
 *
 * The full discriminated union and type guards are defined in the library.
 * OSS consumers should import from this file for backward compatibility,
 * or directly from @datarecce/ui/api.
 */
export type {
  BaseRun,
  Run,
  RunParamTypes,
  RunProgress,
  RunStatus,
  RunType,
} from "@datarecce/ui/api";
