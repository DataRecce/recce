/**
 * @file defaultRenderCell.tsx
 * @description OSS re-export of default cell renderer from @datarecce/ui
 *
 * This file re-exports the defaultRenderCell and related types from @datarecce/ui.
 * Maintained for backward compatibility with existing OSS imports.
 */

export {
  type ColDefWithMetadata,
  defaultRenderCell,
  type RecceColumnContext,
} from "@datarecce/ui/components/ui";
