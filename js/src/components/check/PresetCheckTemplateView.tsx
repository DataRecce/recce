/**
 * Re-export from @datarecce/ui primitives.
 * The generateCheckTemplate and PresetCheckTemplateView are now in the UI library.
 */
export {
  generateCheckTemplate,
  PresetCheckTemplateView,
} from "@datarecce/ui/primitives";
