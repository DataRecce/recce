name: Test Python

permissions:
  contents: read

on:
  push:
    branches:
      - main
    paths:
      - "recce/**" # Recce OSS codebase
      - "tests/**" # Recce OSS python unit tests
  pull_request:
    branches:
      - main
    paths:
      - "recce/**" # Recce OSS codebase
      - "tests/**" # Recce OSS python unit tests
      - "pyproject.toml"

jobs:
  test-flake8:
    name: Check Code Style with Flake8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Install dependencies
        run: uv sync --all-extras --python 3.10

      - name: Run flake8
        run: make flake8

  test-dbt-versions:
    name: Test DBT Versions
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Cache tox environments
        uses: actions/cache@v4
        with:
          path: .tox
          key: ${{ runner.os }}-tox-uv-${{ hashFiles('tox.ini', 'pyproject.toml') }}-3.10
          restore-keys: |
            ${{ runner.os }}-tox-uv-${{ hashFiles('tox.ini', 'pyproject.toml') }}-
            ${{ runner.os }}-tox-uv-

      - name: Install dependencies
        run: uv sync --all-extras --python 3.10

      - name: Run tests
        run: make test-tox

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

  test-python-versions:
    name: Test Python Versions
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [ "3.11", "3.12", "3.13" ]
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Cache tox environments
        uses: actions/cache@v4
        with:
          path: .tox
          key: ${{ runner.os }}-tox-uv-${{ hashFiles('tox.ini', 'pyproject.toml') }}-${{ matrix.python-version }}
          restore-keys: |
            ${{ runner.os }}-tox-uv-${{ hashFiles('tox.ini', 'pyproject.toml') }}-
            ${{ runner.os }}-tox-uv-

      - name: Install dependencies
        run: uv sync --all-extras --python ${{ matrix.python-version }}

      - name: Run test with specified Python version
        run: uv run tox -e ${{ matrix.python-version }}
